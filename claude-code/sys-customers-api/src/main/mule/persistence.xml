<?xml version="1.0" encoding="UTF-8"?>
<mule xmlns="http://www.mulesoft.org/schema/mule/core"
      xmlns:doc="http://www.mulesoft.org/schema/mule/documentation"
      xmlns:file="http://www.mulesoft.org/schema/mule/file"
      xmlns:ee="http://www.mulesoft.org/schema/mule/ee/core"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:schemaLocation="
        http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
        http://www.mulesoft.org/schema/mule/file http://www.mulesoft.org/schema/mule/file/current/mule-file.xsd
        http://www.mulesoft.org/schema/mule/ee/core http://www.mulesoft.org/schema/mule/ee/core/current/mule-ee.xsd">

    <!--
      File-backed mock "table". Behaves like a database while the app runs:
        - readTable: reads the mutable working file. If it does not exist yet
          (first run), seeds it from the classpath seed, writes it, and returns it.
        - writeTable: overwrites the working file with the payload (the full table).
      Callers get/return a List<Customer>; these sub-flows own all file I/O + JSON.
    -->

    <sub-flow name="customers-readTable">
        <try>
            <file:read config-ref="fileConfig" path="${data.file}" outputMimeType="application/json"/>
            <set-payload value="#[payload]" doc:name="table -> payload (List)"/>
            <error-handler>
                <!-- Working file absent (first run): seed from classpath, persist, return. -->
                <on-error-continue type="FILE:ILLEGAL_PATH, FILE:FILE_NOT_FOUND, FILE:ACCESS_DENIED">
                    <set-payload value='#[readUrl("classpath://data/customers.seed.json", "application/json")]' doc:name="load seed"/>
                    <flow-ref name="customers-writeTable"/>
                </on-error-continue>
            </error-handler>
        </try>
    </sub-flow>

    <sub-flow name="customers-writeTable">
        <ee:transform doc:name="table -> JSON string">
            <ee:message>
                <ee:set-payload><![CDATA[%dw 2.0
output application/json
---
payload]]></ee:set-payload>
            </ee:message>
        </ee:transform>
        <file:write config-ref="fileConfig" path="${data.file}" mode="OVERWRITE">
            <file:content>#[payload]</file:content>
        </file:write>
        <!-- Keep the in-memory List as payload for callers that write-then-return. -->
        <set-payload value='#[read(payload, "application/json")]' doc:name="restore List payload"/>
    </sub-flow>
</mule>
